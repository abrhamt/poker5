version: "2"
sql:
  - engine: "mysql"
    schema: "db/schema.sql"
    queries: "db/queries"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "database/sql"
